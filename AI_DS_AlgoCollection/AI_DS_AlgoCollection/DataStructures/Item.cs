namespace AI_DS_AlgoCollection.DataStructures;
/*
public class Item : IComparable<Item>, IComparer<Item> {
    private string itemName;
    public string GetItem() => this.itemName;

    public Item(string itemName) {
        this.itemName = itemName;
    }

    public override bool Equals(object obj) => GetItem().Equals(((Item)obj).GetItem());
    public override int GetHashCode() => GetItem().GetHashCode();
    public override string ToString() => GetItem().ToString();


    public int CompareTo(Item other) {
        return GetItem().CompareTo(other.GetItem());
    }

    public int Compare(Item x, Item y) {
        return x.GetItem().CompareTo(y.GetItem());
    }
}*/